# Backend

This directory contains the Django REST Framework backend for TenantPlus.

## Contents

- `accounts/`, `properties/`, `applications/`, `agreements/` - Django apps.
- `tenantplus/` - Django project configuration package.
- `db.sqlite3` - Local development database.
- `requirements.txt` - Python dependencies for the backend.
